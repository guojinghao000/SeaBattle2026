# 服务端修改记录

> 基于 `Server2026_old`（老师原版）到当前 `Server2026` 的差异

## `Ship.cs`

| 行 | 原版 | 当前 | 分类 |
|---|------|------|------|
| — | 无 `IsBot` | 新增 `public bool IsBot { get; set; }` (L24) | 功能增强 |

## `Form1.cs`

### 1. 文件头

| 行 | 原版 | 当前 | 说明 |
|---|------|------|------|
| 1-5 | `using Microsoft.VisualBasic.ApplicationServices;` / `using System.Xml;` | 已移除 | 清理无用引用 |

### 2. 字段

| 行 | 原版 | 当前 | 分类 |
|---|------|------|------|
| 15 | — | `private readonly object _shipLock = new();` | 健壮性 |
| — | — | `private const int MinBotCount = 5;` (L19) | 功能增强 |
| — | — | `private int _botCycle;` (L20) | 功能增强 |

### 3. 构造函数

| 行 | 原版 | 当前 | 分类 |
|---|------|------|------|
| 30-32 | 注释掉的测试代码 `//shipList.Add(...)` | `StartToolStripMenuItem_Click(this, EventArgs.Empty);` | 功能增强 |

### 4. `PictureBox1_Paint`

| 行 | 原版 | 当前 | 分类 |
|---|------|------|------|
| 36-44 | 直接 `foreach (var ship in shipList)` | `lock → snapshot = shipList.ToList()` 然后遍历快照 | 健壮性 |

### 5. `UpdateTimer_Elapsed`

| 行 | 原版 | 当前 | 分类 |
|---|------|------|------|
| 49 | `if (shipList.Count > 0) { ... }` | `lock → snapshot; if (snapshot.Count == 0) { Invalidate(); return; }` | 健壮性 |
| 51-58 | 直接 `foreach (var ship in shipList)` 调用 `CheckShipHit(shipList, ship)` | 遍历快照，`CheckShipHit(snapshot, ship)` | 健壮性 |
| 70 | `SendToAll(shipList, str)` | `SendToAll(snapshot, str)` | 健壮性 |
| 71 | — | 末尾调用 `RefillBots()` | 功能增强 |

### 6. `StartToolStripMenuItem_Click`

| 行 | 原版 | 当前 | 分类 |
|---|------|------|------|
| 118 | `new TcpListener(serverIP, tcpPort)` | `new TcpListener(IPAddress.Any, tcpPort)` | **Bug 修复** |
| 121 | `AddMessage($"开始侦听{serverIP}:{tcpPort}...")` (乱码) | 同上 + `this.Text = $"大海战 服务端 0.0.1 - {serverIP}:{tcpPort}"` | 功能增强 |
| — | — | 末尾调用 `SpawnBots()` | 功能增强 |

### 7. `ListenClientConnect`

| 行 | 原版 | 当前 | 分类 |
|---|------|------|------|
| 148 | `shipList.Add(ship)` | `lock (_shipLock) { shipList.Add(ship); }` | 健壮性 |

### 8. `ReceiveData`

| 行 | 原版 | 当前 | 分类 |
|---|------|------|------|
| 176-178 | 异常时 `RemoveShip(ship)` 后无资源清理 | 异常时 `RemoveShip(ship)` | 不变 |
| 196-197 | `case "logout"`: 仅 `exitWhile = true` | 增加 `ship.moveTimer.Stop(); ship.fireTimer.Stop();` | **Bug 修复** |
| 213-222 | `case "fire"`: `ship.fx = result.limitedX; ship.fy = result.limitedY;` (相对偏移) | `ship.fx = ship.px + result.limitedX; ship.fy = ship.py + result.limitedY;` (加舰船位置) | **Bug 修复** |
| 234-243 | 循环后：`Client.Close(); RemoveShip; SendToAll(shipList, ...)` | 停止 timer → 关闭 Writer/Reader/Client → `RemoveShip` → `Invalidate()` → 锁快照 → `SendToAll(remaining, ...)` | 健壮性 |

### 9. `GetAllShipName`

| 行 | 原版 | 当前 | 分类 |
|---|------|------|------|
| 258-262 | 直接 `foreach (var ship in shipList)` | `lock → snapshot` 然后遍历快照 | 健壮性 |

### 10. `RemoveShip`

| 行 | 原版 | 当前 | 分类 |
|---|------|------|------|
| 267-271 | `if (Contains) Remove` (无锁) | `lock (_shipLock) { if (Contains) Remove; }` | 健壮性 |

### 11. 新增方法

| 方法 | 说明 | 分类 |
|------|------|------|
| `SpawnBots()` | 创建 5 个机器人靶船（`IsBot = true`，停 timer），加入 `shipList`，广播 `Online` | 功能增强 |
| `RefillBots()` | 检查 `IsBot` 计数 < 5 时补充差额 | 功能增强 |
| `Form1_FormClosing` | 窗口关闭时停 timer、关连接、清 `shipList`、`Environment.Exit(0)` | 健壮性 |

### 12. `Form1_FormClosing`

| 行 | 原版 | 当前 | 分类 |
|---|------|------|------|
| — | 无此方法 | 新增 `Form1_FormClosing` 事件处理 | 健壮性 |

### 13. 注释/日志乱码修复

| 位置 | 原版 | 当前 | 分类 |
|------|------|------|------|
| 全文件 | 约 30 处中文注释/`AddMessage` 字符串为乱码（GBK 编码被读作 UTF-8） | 全部恢复为正确简体中文 | **Bug 修复** |

## `Form1.Designer.cs`

| 行 | 原版 | 当前 | 分类 |
|---|------|------|------|
| — | 无 `FormClosing` 订阅 | 新增 `FormClosing += Form1_FormClosing;` | 健壮性 |

---

## 改动汇总

| 分类 | 数量 | 明细 |
|------|------|------|
| **Bug 修复** | 3 | ① TCP 绑定 `IPAddress.Any` ② 炮击坐标加舰船位置 ③ 中文乱码 |
| **功能增强** | 6 | ① 机器人靶船 ② 自动启动监听 ③ 标题栏显示 IP ④ `IsBot` 属性 ⑤ `SpawnBots` ⑥ `RefillBots` |
| **健壮性** | 10 | ①~⑤ `shipList` 加锁(5处) ⑥ `logout` 停 timer ⑦ 空列表 `Invalidate` ⑧ 资源完整释放 ⑨ `FormClosing` 退出 ⑩ 清理无用引用 |
